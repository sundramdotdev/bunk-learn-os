import puppeteer from "puppeteer"; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on("console", msg => { if (msg.type() === "error") console.log("PAGE ERROR LOG:", msg.text()) }); page.on("pageerror", error => console.log("PAGE ERROR:", error.message)); await page.goto("http://127.0.0.1:5174/"); await new Promise(r => setTimeout(r, 2000)); const texts = await page.evaluate(() => Array.from(document.querySelectorAll("button")).map(b => b.textContent)); for (const text of texts) { if (text && text.trim().length > 0 && text !== "Reset" && text !== "Format OS") { console.log("Clicking", text); await page.evaluate((t) => { const btn = Array.from(document.querySelectorAll("button")).find(b => b.textContent === t); if(btn) btn.click(); }, text); await new Promise(r => setTimeout(r, 500)); } } await browser.close(); })();
